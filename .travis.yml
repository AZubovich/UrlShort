dist: bionic
language: ruby
rvm:
- 2.6.3
before_script:
  - cp config/database.yml.travis config/database.yml
script:
 - rake db:test:prepare
 - bundle exec rspec
 - bundle exec rubocop
services:
  - redis-server
  - postgresql